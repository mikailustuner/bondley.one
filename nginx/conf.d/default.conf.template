# --- HTTP Server (always available, for ACME challenge and fallback) ---
# API subdomain
server {
    listen 80;
    server_name api.${DOMAIN};

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# Web subdomains (dashboard, admin, landing)
server {
    listen 80;
    server_name dashboard.${DOMAIN} admin.${DOMAIN} ${DOMAIN} www.${DOMAIN};

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://web:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- HTTPS Servers (only if SSL cert exists) ---
# These will be included only if certbot certificates are available
# For now, we'll use HTTP fallback above

# Note: SSL server blocks are commented out until certificates are obtained
# Uncomment these after running certbot successfully

# server {
#     listen 443 ssl;
#     server_name api.${DOMAIN};
#     ssl_certificate /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;
#     include /etc/nginx/ssl-params.conf;
#     location / {
#         proxy_pass http://api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 75s;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name dashboard.${DOMAIN} admin.${DOMAIN} ${DOMAIN} www.${DOMAIN};
#     ssl_certificate /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;
#     include /etc/nginx/ssl-params.conf;
#     location / {
#         proxy_pass http://web:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
