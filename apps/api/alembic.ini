# Alembic configuration for FinCalc API
# Run from apps/api: alembic upgrade head
# URL is set in env.py from application config (no sqlalchemy.url here)

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

[post_write_hooks]
# hooks = black
# black.type = console_scripts
# black.entrypoint = black
